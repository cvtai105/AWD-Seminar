name: Deploy Identity ce to Azure Virtual Machine 

on:
  push:
    branches: [ main ]
    

env:
  version: 1.0.0

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout ⚙️
        uses: actions/checkout@v4
      
      - name: Make .env file 📄
        run: |
          echo "IDENTITY_API_VERSION=${{ env.version }}" > .env

          echo "CORS_ALLOWED_ORIGINS=*" >> .env

          echo "DB_CONNECTION_STRING=Host=ep-summer-mode-a1edo8x9.ap-southeast-1.aws.neon.tech;Port=5432;Database=verceldb;Username=default;Password=3Qkce7WuiHDo;SSL Mode=Require" >> .env

          echo "GOOGLE_CLIENT_ID=907759503353-5v3aon6s0fdhklod6qsc6bedrt3vketk.apps.googleusercontent.com" >> .env
          echo "GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}" >> .env
          
          echo "JWT_SECRET=test-secret-key" >> .env
          echo "JWT_ISSUER=http://localhost:8080" >> .env
          echo "JWT_AUDIENCE=http://localhost:8080" >> .env

      - name: Build images 🐳
        run: |
          docker compose -f docker-compose.host.yml build identity-api

      - name: Login to Docker Hub 🐳
        uses: docker/login-action@v2
        with:
          username: cvtai
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push images 🐳
        run: |
          docker compose push identity-api

      - name: copy .env file to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: .env
          target: /Seminar-CI-CD/.env

      - name: copy compose file to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: docker-compose.yml
          target: /Seminar-CI-CD/docker-compose.yml

      - name: Pull and Deploy by SSH 🚀
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker pull cvtai/seminar-identity-api:${{ env.version }}
            cd Seminar-CI-CD
            docker compose down identity-api database
            docker compose up identity-api database -d 